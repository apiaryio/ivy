box: node:8
services:
  - redis
build:
  steps:
    - script:
        name: env
        code: env
    - npm-install
    - script:
        name: build coffee
        code: ./scripts/build
    - npm-test
  # Slack notification options https://github.com/wercker/step-slack
  after-steps:
    - slack-notifier:
      url: $SLACK_URL

